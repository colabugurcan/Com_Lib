#include <comm/core/interface.hpp>

namespace comm {
namespace detail {
// Placeholder compilation unit to keep the static library target valid
// until concrete transport implementations are added.
void keep_static_library_linkable() {}
} // namespace detail
} // namespace comm
